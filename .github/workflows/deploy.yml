name: Deploy to GitHub Pages (deprecated)

on:
  # Disable automatic triggers to avoid duplicate Pages deploy runs.
  # Keep only manual dispatch for emergency use.
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages-deprecated"
  cancel-in-progress: true

jobs:
  # Build job
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      # 정적 사이트이므로 빌드/의존성 설치 불필요. Pages 아티팩트만 업로드합니다.

      - name: Setup Pages
        uses: actions/configure-pages@v4

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: '.'

  # Deployment job
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4

      - name: Send success notification
        if: success()
        run: |
          echo "✅ 배포 성공!"
          echo "🌐 URL: ${{ steps.deployment.outputs.page_url }}"

      - name: Send failure notification
        if: failure()
        run: echo "❌ 배포 실패!"
